===
version: 2
jobs:
  pr_check:
    docker:
      - image: circleci/node:8.10.0
    steps:
      - checkout
      - run: npm install
      - run: npm test

workflows:
  version: 2
  pr-check:
    jobs:
    - pr_check
  build-deploy:
    jobs:
    - build:
        filters:
          branches:
            only:
            - master